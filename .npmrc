# pnpm configuration
# Minimum package age: 60 days (86400 minutes) to mitigate supply chain attacks
# See: https://pnpm.io/supply-chain-security
minimum-release-age=86400

# If you need to bypass the age restriction for a specific package due to
# a critical security fix, add it to this comma-separated list:
# minimum-release-age-exclude=package-name,another-package

# Enable scripts for packages that need native compilation
# pnpm v10.16+ has a security feature that blocks build scripts by default
# For CI/CD, use: pnpm install --config.ignore-scripts=false
enable-pre-post-scripts=true
